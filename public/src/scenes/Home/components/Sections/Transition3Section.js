import React, {Component} from "react";
import ReactSVG from 'react-svg';
import {Link} from "react-router-dom";

export default class Transition3Section extends Component{

  constructor(props){
    super(props);
  }

  render(){
    const {imgDir} = this.props;
    return(
      <div></div>
    )
  }
}
